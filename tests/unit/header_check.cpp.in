// Generated by tests/CMakeLists.txt from tests/unit/header_check.cpp.in - do
// not edit, and do not add anything to it.
//
// One of these exists per public platform header, and it includes exactly one
// header and nothing else. A header that forgot an include of its own still
// compiles inside the unit test binary, where some earlier include happened to
// supply the missing declaration; here there is nothing to borrow from, so the
// omission is a build failure instead of a trap for the first consumer that
// includes it first.
#include "@DRAWGUI_HEADER_CHECK_INCLUDE@"
