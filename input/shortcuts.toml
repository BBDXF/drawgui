# drawgui shortcut/intent definitions - the single source of truth for
# design.md section 5.5.1's central rule: application code never writes
# `Ctrl+C`, it declares an intent (`Intent::Copy`) and a per-platform
# binding table resolves it to a real chord. Generated by
# tools/gen_shortcuts.py into:
#
#   include/drawgui/shortcuts/action_ids.generated.h    dg_action_id constants
#   include/drawgui/shortcuts/logical_key.generated.h   the LogicalKey enum
#   include/drawgui/shortcuts/action_scope.generated.h  the ActionScope enum
#   src/shortcuts/logical_key_table.generated.inc       name -> LogicalKey
#   src/shortcuts/binding_table.generated.inc           action -> chord table
#   doc/shortcuts.generated.md                          a generated reference
#
# This is the fourth id-generator family (props/, themes/, abi/, this one),
# same shape as themes/schema.toml on purpose (design.md section 5.5.3:
# action_id is "同源同构" with prop_id/token_id - a uint16_t PLAIN CONSTANT,
# never an enum, because it crosses the C ABI the same way they do).
#
# NUMBERING IS AN ABI CONTRACT, identical to the other three families: `id`
# is a uint16_t, assigned explicitly, APPEND-ONLY. id 0 is reserved for
# DG_ACTION_INVALID. input/action_ids.lock guards against renumbering the
# same way props/prop_ids.lock and themes/token_ids.lock already do.
#
# WHAT `LogicalKey` IS, AND WHY IT IS NOT `dg::Key`
# (include/drawgui/window/window_manager.h): `Key` names TEXT-EDITING
# intents a widget consumes directly (arrow/Home/End/Backspace...) - design.md
# section 5.5.2 is explicit that a text-editing key is not a shortcut, they
# route at different levels, and this project does not unify them. LogicalKey
# is CLOSED and INTERNAL (never arrives as an arbitrary host-supplied number,
# unlike action_id), so it stays a real `enum class`, generated from exactly
# the key names a [[action]] below references - "no enumerator without a
# real consumer" becomes mechanical instead of a rule someone has to
# remember, matching how `Key` itself already declines to carry a key
# nothing routes.
#
# THE `Mod` PSEUDO-MODIFIER (design.md section 5.5.1): most rows need write
# only ONE binding, `Mod+<key>`, covering all three platforms - the
# generator substitutes `Mod -> Ctrl` for linux/windows and `Mod -> Cmd` for
# macos. This is a DATA substitution rule inside a generator that has to
# exist for Linux anyway, not hand-written untested macOS application code,
# so it is honestly testable today with no macOS machine at all
# (tests/unit/test_shortcuts.cpp does exactly that).
#
# DECLINED FOR THIS SLICE, BY NAME (design.md section 5.5.1's own examples):
#   - macOS's `Ctrl+A`/`Ctrl+E` meaning line-start/line-end (a system-level
#     emacs binding, NOT this project's `select_all`) - not `Mod`-derivable,
#     and there is no macOS backend to test the override against yet.
#   - macOS's `Option+Delete` for "delete previous word" where
#     Windows/Linux use `Ctrl+Backspace` - same reason, and this project has
#     no delete-word action at all yet (it belongs to TextField's own
#     editing intents per section 5.5.2, not to this table).
# Both are TextField editing-intent overrides, not app/clipboard shortcuts,
# so neither has a row here to override in the first place; they are
# recorded so a future macOS backend slice does not have to rediscover them.
#
# FIELD REFERENCE:
#
#   name      required. snake_case, matches ^[a-z][a-z0-9_]*$. The generated
#             constant is DG_ACTION_<NAME uppercased>.
#   id        required. uint16_t, see numbering rules above.
#   scope     required. Which routing level (design.md section 5.5.2) this
#             action's binding lives at:
#               text_field  - level 2, a focused TextField's own bubbling
#                             scope; beats an app-level binding for the same
#                             chord once 8-3 builds the router.
#               app         - level 4, the application-wide table.
#             Generates ActionScope (action_scope.generated.h) from exactly
#             the scope values used below - same "no speculative vocabulary"
#             policy as LogicalKey.
#   consumer  required, non-empty. Names the slice/mechanism that consumes
#             this action. THE GENERATOR FAILS if this is empty or missing -
#             this is deliberately stricter than props/themes/abi, because
#             it is what turns "interfaces are extracted from a working
#             implementation, never written ahead of one" into something
#             the build enforces rather than something a reviewer has to
#             notice.
#   binding   required. One `Mod`/`Shift`/`Alt` combination (in that fixed
#             order, joined by '+') followed by a logical key name, e.g.
#             "Mod+C", "Mod+Shift+C", or a bare key with no modifier
#             ("PageUp"). No per-platform override table exists yet - see
#             the declines above.
#   summary   required. One-line human description.

schema_version = 1

[meta]
# The id the next new action must take. Bump it when appending.
next_id = 9

# ---------------------------------------------------------------------------
# Clipboard (8-4) + TextField's own select-all. All four share the
# text_field scope: they only mean something with a focused text field, and
# design.md section 5.5.2 requires a component-level binding to beat an
# app-level one for the same chord once the router (8-3) exists.
# ---------------------------------------------------------------------------

[[action]]
name = "copy"
id = 1
scope = "text_field"
consumer = "8-4 clipboard"
binding = "Mod+C"
summary = "Copy the current selection to the clipboard."

[[action]]
name = "cut"
id = 2
scope = "text_field"
consumer = "8-4 clipboard"
binding = "Mod+X"
summary = "Cut the current selection to the clipboard."

[[action]]
name = "paste"
id = 3
scope = "text_field"
consumer = "8-4 clipboard"
binding = "Mod+V"
summary = "Paste the clipboard's text at the caret."

[[action]]
name = "select_all"
id = 4
scope = "text_field"
consumer = "8-4 clipboard + TextField"
binding = "Mod+A"
summary = "Select the field's entire text."

# ---------------------------------------------------------------------------
# Keyboard scrolling (8-3c), closing doc/scrolling.md section 4's named
# decline ("design.md routes PageUp/Down/Home/End through the intent
# mechanism (5.5.1). 5.5.1 does not exist" - it does now). `app` scope: no
# ScrollView registers a component-level scope in this slice (that is 8-3's
# ActionScopes side table, not built here), so these bind at the
# application-wide level until a real consumer narrows them.
# ---------------------------------------------------------------------------

[[action]]
name = "scroll_page_up"
id = 5
scope = "app"
consumer = "8-3c keyboard scrolling"
binding = "PageUp"
summary = "Scroll the target scrollable up by one page."

[[action]]
name = "scroll_page_down"
id = 6
scope = "app"
consumer = "8-3c keyboard scrolling"
binding = "PageDown"
summary = "Scroll the target scrollable down by one page."

[[action]]
name = "scroll_to_start"
id = 7
scope = "app"
consumer = "8-3c keyboard scrolling"
binding = "Home"
summary = "Scroll the target scrollable to its content start."

[[action]]
name = "scroll_to_end"
id = 8
scope = "app"
consumer = "8-3c keyboard scrolling"
binding = "End"
summary = "Scroll the target scrollable to its content end."
